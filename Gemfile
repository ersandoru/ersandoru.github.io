source "https://rubygems.org"

# This is the recommended gem for hosting on GitHub Pages
gem "github-pages", group: :jekyll_plugins

# This will help ensure the proper Jekyll version is running.
# The github-pages gem will install the correct version of Jekyll.

# To use this Gemfile, run `bundle install`, then `bundle exec jekyll serve`.